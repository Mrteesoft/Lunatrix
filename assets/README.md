Current landing background asset:

`frontend/assets/keto-signal-bloom.svg`

The page animates this image with slow CSS drift to create the moving background effect.

If you want to swap the artwork, update the background image path used in `frontend/styles.scss`.
